# فقاعة مكالمة الهاتف: LTR عمدًا

`drawChatMessageHook` هو مسار مستقل لفقاعة المكالمة الصوتية. في نسخة الاختبار الحالية لا يُطبّق عليه أي helper من محاذاة RTL، ولذلك يبقى على تخطيط اللعبة الأصلي LTR.

هذا القرار مؤقت ومحافظ: الفقاعة قد تحتوي توزيعًا خاصًا لرسائل متحدثين مختلفين داخل نفس الإطار، لذلك لا نريد جمع كل المقاطع أو فرض حافة RTL واحدة قبل رؤية شكلها بعد التعريب.

المسارات الأخرى مستقلة:

- الحوار العام يبقى RTL مع الكتابة التدريجية.
- نص الهاتف العام يبقى انتقائيًا؛ عبارات الحالة والأسماء القصيرة LTR.
- `@Channel` والبريد التفاعلي يستخدمان إعداداتهما المستقلة.

إذا بدا شكل فقاعة المكالمة مناسبًا بعد ترجمة النص، تُترك كما هي. وإذا احتاجت RTL لاحقًا، ينبغي إضافة محاذاة مستقلة لكل مقطع/متحدث بدل تعديل `rtlDialogueRightX` أو `rtlPhoneRightX`.
