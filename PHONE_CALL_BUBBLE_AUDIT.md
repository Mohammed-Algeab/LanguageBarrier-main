# تدقيق فقاعة مكالمة الهاتف

## النتيجة الحالية

فقاعة مكالمة الهاتف لا تمر عبر `drawDialogueHook` العام. في مسار RNE/RND يتم ربط `drawChatTextBox` بالدالة `drawChatMessageHook`، ويرتبط اسم المتصل منفصلًا بالدالة `drawPhoneCallNameHook`.

`drawChatMessageHook` يستخدم `processSc3TokenList` مباشرة، ولا يطبق حاليًا `rtlAlignTextLines` ولا `dialogueLineShiftX` ولا منطق الكتابة التدريجية الخاص بالحوار. لذلك لا ينبغي إصلاح المشكلة عبر تغيير `rtlDialogueRightX` أو محاذاة `drawPhoneTextHook`.

## الملاحظة من الصور

في فقاعة المكالمة تظهر أجزاء النص في مواضع أفقية مختلفة داخل الفقاعة. من الكود لا يمكن الجزم هل كل جزء جاء من استدعاء مستقل لـ`drawChatMessageHook` أم من تقسيم داخلي قبل الوصول إلى الـhook؛ لذلك الإصلاح الآمن يجب أن يطبق محاذاة مستقلة على `ProcessedSc3String_t` بعد التخطيط، باستخدام `a3` و`a5` الخاصين بالفقاعة، دون لمس الهاتف العام أو الحوار.

## مسار الإصلاح المقترح

إضافة إعداد مستقل مثل `rtlPhoneCallBubble`، ثم بعد استدعاءي `processSc3TokenList` في `drawChatMessageHook` تطبيق helper محاذاة خاص بالفقاعة. يجب اشتقاق الحافة اليمنى من `a3 + a5` وبالمقياس المستخدم في هذا المسار، مع عدم استخدام `RTL_PHONE_RIGHT_X` العام. كما يجب إبقاء LTR كخيار افتراضي/احتياطي إذا لم يكن نوع اللعبة أو حدود الصندوق مؤكدًا.
