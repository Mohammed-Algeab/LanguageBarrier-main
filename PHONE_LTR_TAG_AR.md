# وسم `[phone-ltr]` لفقاعة مكالمة الهاتف في SGHD

هذا الوسم مخصص للنص الطويل الذي يظهر بجانب الهاتف أثناء المكالمة. هذه الفقاعة تمر عبر مسار رسم الحوار العام في SGHD، ولذلك يتأثر موضعها بـ`rtlDialogue`. عند وضع `[phone-ltr]` في بداية السطر النصي الخاص بالفقاعة، يحتفظ هذا السطر بمسار LTR الأصلي حتى لو بقي `rtlDialogue` مفعّلًا للحوار العادي.

## الاستخدام في ملف COZ

```text
[phone-ltr]النص الطويل الذي يظهر بجانب الهاتف
```

لا تعكس النص، ولا تضف Arabic shaping، ولا تستخدم الوسم في الحوار العادي أو في اسم المتصل أو في عبارة `Calling`. النص العربي في هذا المشروع ممثل أصلًا بترتيب الرموز الذي يتطلبه خط اللعبة.

## الترميز داخل SC3

يحوّل sc3tools الوسم إلى زوج النص الخام `8B 2D`، أي الكلمة `0x8B2D`. في charset الخاص بـSGHD يقابل هذا الموضع الرمز الخاص `U+E06D`، وهو ضمن منطقة الرموز الخاصة المستخدمة كـplaceholder/compound mapping وليس opcode. يعامله sc3tools كـ`StringToken::PhoneLtr`، ثم يعيد عرضه عند الاستخلاص بصيغة `[phone-ltr]`.

قبل word-wrap والرسم، تكتشف DLL الزوج `8B 2D` وتحذفه من النص المرئي، وتحتفظ بحالة `forceLTR` للسلسلة الحالية. عند وجود هذه الحالة، يعطّل مسار الحوار فقط إزاحة RTL، وعكس كشف الأحرف التدريجي، وملاءمة glyph الخاصة بـRTL. لا يتغير النص المصدر ولا ترتيب رموزه، ولا تتأثر محاذاة الهاتف والبريد الإلكتروني أو الحوار العادي.

## الاختبار

اختبارات sc3tools المضافة هي `phone_ltr_marker_roundtrip` و`phone_ltr_tag_roundtrip_sghd`. تتحقق الأولى من فك marker الخام كـtoken مستقل، وتتحقق الثانية من تحويل `[phone-ltr]` إلى `8B 2D` ثم إعادته إلى الوسم. البناء النهائي لـDLL يجب أن يتم عبر GitHub Actions باستخدام Visual Studio وWin32/x86 كما في workflow الحالي.
