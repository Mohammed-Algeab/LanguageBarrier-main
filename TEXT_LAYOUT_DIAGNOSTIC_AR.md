# دليل تشخيص مسارات الفقاعة وBacklog

هذه النسخة مخصصة لتحديد مسار الرسم الفعلي قبل إضافة أي RTL جديد. لا تغيّر مواضع النص، ولا تعكس SC3، ولا تضيف Arabic shaping، ولا تطبق محاذاة جديدة. يجب استخدام DLL التشخيص مع إعداد `textLayoutDiagnosticLog: true` الموجود في `TEXT_LAYOUT_DIAGNOSTIC_PATCHDEF_SNIPPET.json`، ثم دمج المفتاحين داخل كائن `patch` في patchdef المستخدم للعبة.

## ما الذي يسجله DLL؟

يكتب السجل أسطرًا تبدأ بالوسم `TEXT_DIAG[...]` داخل ملف السجل المعتاد للعبة. كل فئة محدودة بعدد أحداث لتجنب الكتابة في كل إطار.

| الوسم | ما يكشفه |
|---|---|
| `TEXT_DIAG[chat]` | هل وصلت فقاعة المكالمة إلى `drawChatMessageHook`، وما طول صندوقها وعدد الأسطر وبيانات SC3 المختصرة |
| `TEXT_DIAG[backlog]` | هل وصل Backlog إلى hook الرسم المخصص، وأي نسخة من المسار استُخدمت، مع موضع الرسم وعدد الأسطر المخزنة |
| `TEXT_DIAG[phone]` | معاملات جسم الهاتف، وعدد الأسطر الفعلية، وهل طُبقت محاذاة RTL أم حُجبت |
| `TEXT_DIAG[callName]` | مسار اسم المتصل ومعاملات صندوقه وبيانات SC3 |
| `TEXT_DIAG[singleLine]` | استدعاءات النص الأحادي، وعنوان العودة الذي يساعد على معرفة ما إذا كانت `Calling` أو Backlog تمر بهذا المسار |
| `TEXT_DIAG[width]` | استدعاءات حساب عرض النص، وهي مفيدة لكشف اختلاف قياس الخط المستبدل عن قياس الالتفاف |
| `TEXT_DIAG[lineCount]` | استدعاءات حساب عدد الأسطر المستخدمة لتحديد حدود التمرير |
| `TEXT_DIAG[mail]` | مسار @Channel والبريد التفاعلي وعدد الروابط والأسطر |

## طريقة التشغيل

1. ابنِ DLL من GitHub Actions باستخدام إعداد `dinput8-Release | Win32`.
2. خذ الـDLL الناتج من الـArtifact وضعه في مجلد اللعبة بدل DLL الإصدار العادي.
3. استخدم نسخة حفظ تصل إلى فقاعة المكالمة الطويلة، ثم اترك الفقاعة تظهر كاملة مرة واحدة.
4. افتح Backlog، انتقل بين صفحتين أو أكثر، ثم اخرج منه. إذا أمكن، افتح @Channel أيضًا حتى نميز المسار المشترك.
5. أغلق اللعبة بالكامل حتى يُغلق ملف السجل.
6. أرسل ملف السجل المعتاد للعبة، أو انسخ جميع الأسطر التي تحتوي على `TEXT_DIAG[`.

## ما نبحث عنه

إذا ظهرت أسطر `chat` في SGLBP، فلدينا مسار قابل لإصلاح التفاف الفقاعة دون تخمين signature جديدة. إذا لم تظهر، فهذا يثبت أن فقاعة SGLBP تستخدم مسارًا آخر، وسنحتاج إلى تحديده من سجلات `singleLine` أو `width` أو `lineCount` قبل تركيب hook.

إذا ظهرت أسطر `backlog`، فلدينا hook Backlog فعلي ويمكن دراسة إزاحة السطر إلى اليمين في إصدار مستقل. إذا لم تظهر أسطر `backlog` لكن ظهرت أسطر `singleLine` أو `width` أثناء فتح Backlog، فسيكون Backlog معتمدًا على مسار نص عام، وقد نستطيع إضافة محاذاة انتقائية له. أما غياب جميع هذه السجلات فيعني أن SGLBP يرسم Backlog من مسار غير مربوط حاليًا في LanguageBarrier.

> لا ترسل ملف `game.exe` أو أي ملف تنفيذي للعبة. المطلوب فقط تشغيل DLL وإرسال السجل الناتج.

## تعطيل التشخيص

بعد انتهاء الاختبار، أعد `textLayoutDiagnosticLog` إلى `false` أو احذف المفتاح من patchdef، ثم استخدم DLL الإصدار العادي. وضع التشخيص لا يغيّر الرسم لكنه يزيد حجم السجل ويُستخدم للاختبار فقط.
